# DEPRECATED: ReelForge now uses pyproject.toml.
#
# Install with one of:
#   pip install -e .            # editable install
#   pip install -e ".[dev]"     # with dev tools
#   uv sync                     # if you use uv (recommended)
#
# This file is kept as a no-op pointer for legacy CI and IDE detection.
-e .
